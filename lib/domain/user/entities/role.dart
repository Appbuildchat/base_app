// 사용자 역할을 정의하는 열거형(enum)
//
// 이 파일은 사용자의 역할(Role)을 나타내는 enum을 정의합니다.
// 사용자의 권한 구분, 화면 구성 분기 등에 활용됩니다.
//
// 사용 방법:
// 1. UserEntity와 같은 모델에서 역할을 표현할 때 사용합니다.
// 2. 화면(UI)이나 라우팅 분기 등에서 역할별 처리가 필요할 때 활용합니다.
//
// 사용 예시:
//   Role.admin
enum Role { admin, user }
